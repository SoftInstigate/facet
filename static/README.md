# Static Assets

This folder is mounted into the Facet container at `/opt/restheart/static`.

- `favicon.ico` is served at `/favicon.ico` to avoid browser auth popups.
- Add CSS, images, and other static files here.
