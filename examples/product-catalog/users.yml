# User accounts for Product Catalog example
# Password format: plain text (for development only!)

users:
  - userid: admin
    password: secret
    roles:
      - admin

  - userid: viewer
    password: viewer
    roles:
      - viewer

