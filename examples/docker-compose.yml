# Docker Compose for Facet Product Catalog Example
# Run from the examples directory with: docker-compose up

services:
  mongodb:
    image: mongo:8.0
    container_name: facet-examples-mongo
    #ports:
    #  - "27017:27017"
    volumes:
      - mongo-data:/data/db
      # Mount product catalog init script to populate sample data
      - ./product-catalog/init-data.js:/docker-entrypoint-initdb.d/init-data.js:ro
    networks:
      - facet-network
    healthcheck:
      test: echo 'db.runCommand("ping").ok' | mongosh localhost:27017/test --quiet
      interval: 10s
      timeout: 5s
      retries: 5

  facet:
    build:
      context: ..
      dockerfile: examples/Dockerfile
    image: facet:latest
    container_name: facet-examples
    depends_on:
      mongodb:
        condition: service_healthy
    ports:
      - "8080:8080" # RESTHeart API HTTP port
    environment:
      RHO: >
        /mclient/connection-string->"mongodb://mongodb";
        /pebble-template-processor/enabled->true;
        /http-listener/host->"0.0.0.0";
    volumes:
      # Mount product catalog configuration (overrides baked-in base config)
      - ./product-catalog/restheart.yml:/opt/restheart/etc/restheart.yml:ro
      # Mount users file for authentication
      - ./product-catalog/users.yml:/opt/restheart/etc/users.yml:ro
      # Mount product catalog templates (hot-reload enabled)
      - ./product-catalog/templates:/opt/restheart/templates:ro
      # Mount product catalog static assets (CSS, images, etc.)
      - ./product-catalog/static:/opt/restheart/static:ro
    networks:
      - facet-network
    healthcheck:
      test: curl -f http://localhost:8080/ping || exit 1
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

volumes:
  mongo-data:
    name: facet-examples-mongo-data

networks:
  facet-network:
    name: facet-examples-network
    driver: bridge
